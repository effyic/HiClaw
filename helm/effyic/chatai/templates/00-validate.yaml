{{- if not (.Values.postgres.database | default "") -}}
{{- fail "postgres.database is required (shared PostgreSQL database for Agno session + tenant config)" -}}
{{- end -}}

{{- if not (.Values.hiclaw.releaseName | default "") -}}
{{- fail "hiclaw.releaseName is required (name of the existing HiClaw Helm release)" -}}
{{- end -}}
